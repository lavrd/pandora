syntax = "proto3";

package tracker;

service Tracker {
  rpc GetValidator (Empty) returns (BrokerOpts) {}
  rpc NewValidator (BrokerOpts) returns (Empty) {}
}

message Empty {}

message BrokerOpts {
  string endpoint = 1;
  string user = 2;
  string password = 3;
}

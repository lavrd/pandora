syntax = "proto3";

package tracker;

service Tracker {
  rpc GetBrokerOpts (Empty) returns (BrokerOpts) {}
}

message Empty {}

message BrokerOpts {
  string endpoint = 1;
  string user = 2;
  string password = 3;
}
